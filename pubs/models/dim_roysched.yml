models:
  - name: dim_roysched
    description: Schedule of Royalty.One row per title royalty and title_id.
    columns:
      - name: royschedkey
        description: The surrogate key for the royalty
        tests:
          - not_null
          - unique
      - name: title_id
        description: fk to source table
        tests:
          - not_null
      - name: lorange
        description: low range
      - name: hirange
        description: high range
      - name: royalty
        description: Royalty percentage